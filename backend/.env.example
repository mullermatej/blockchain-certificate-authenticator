# Environment Configuration for Blockchain Certificate Authenticator Backend

# ================================
# BLOCKCHAIN CONFIGURATION
# ================================

# Smart Contract Address (replace after deployment)
# Example: CONTRACT_ADDRESS=0x1234567890abcdef1234567890abcdef12345678
CONTRACT_ADDRESS=0x...

# RPC Provider URL for Polygon Amoy Testnet (Mumbai deprecated)
# Option 1: Use free public endpoint (may be slower/less reliable)
PROVIDER_URL=https://rpc-amoy.polygon.technology

# Option 2: Use Infura (recommended - sign up at infura.io)
# PROVIDER_URL=https://polygon-amoy.infura.io/v3/YOUR_PROJECT_ID

# Option 3: Use Alchemy (alternative - sign up at alchemy.com)
# PROVIDER_URL=https://polygon-amoy.g.alchemy.com/v2/YOUR_API_KEY

# ================================
# SERVER CONFIGURATION
# ================================

# Port for the backend server
PORT=3001

# ================================
# SECURITY & DEPLOYMENT
# ================================

# Node environment (development/production)
NODE_ENV=development

# CORS origin (for production, specify your frontend domain)
# CORS_ORIGIN=https://your-frontend-domain.com
CORS_ORIGIN=http://localhost:5173

# ================================
# INSTRUCTIONS
# ================================

# 1. Copy this file to `.env` in the backend directory
# 2. Replace CONTRACT_ADDRESS with your deployed contract address
# 3. (Optional) Replace PROVIDER_URL with your Infura/Alchemy URL for better reliability
# 4. Never commit the .env file to version control if it contains sensitive API keys
